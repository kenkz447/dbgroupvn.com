import { ADMIN_ROUTE_NAME, ADMIN_ROUTE_PATH } from './../keys'
import { ViewRoute } from '../../shared/core'

export const AdminRoute: ViewRoute = {
    path: ADMIN_ROUTE_PATH,
    name: ADMIN_ROUTE_NAME,
}